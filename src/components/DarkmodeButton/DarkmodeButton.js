import * as React from 'react'

import * as style from './DarkModeButton.module.css'
import '../layout.css'

const DarkModeButton = () => {
	return <button className={style.darkmodeButton}>&#127774;</button>
}

export default DarkModeButton
